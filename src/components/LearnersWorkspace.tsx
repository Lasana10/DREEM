import { useState, type ReactNode } from "react";
import { BadgeCheck, BookOpenCheck, BusFront, CalendarCheck, CircleDollarSign, FolderHeart, IdCard, Printer, ShieldCheck, UserRound, UsersRound, X } from "lucide-react";
import type { LearnerSummary, Role, SchoolBrand } from "../domain/types";
import { loadLearnerOneFile, type LearnerOneFileData } from "../lib/repository";
import { EmptyState } from "./Shell";

const money=(value:number)=>new Intl.NumberFormat("fr-FR").format(value)+" FCFA";
export default function LearnersWorkspace({learners,brand,role}:{learners:LearnerSummary[];brand:SchoolBrand;role:Role}){const[selected,setSelected]=useState<LearnerOneFileData|null>(null),[busyId,setBusyId]=useState(""),[error,setError]=useState("");async function open(id:string){setBusyId(id);setError("");try{setSelected(await loadLearnerOneFile(id))}catch(reason){setError(reason instanceof Error?reason.message:"Learner OneFile could not be loaded.")}finally{setBusyId("")}}return <div className="content"><section className="page-intro"><div><span>DREEM ONEFILE</span><h2>One learner. One continuous institutional record.</h2><p>Identity, guardians, learning, attendance, care, finance, credentials and transport remain connected without exposing records outside the viewer&apos;s rights.</p></div></section>{error&&<div className="form-status error">{error}</div>}{learners.length===0?<EmptyState title="No learners yet" body="Complete an accepted admission or approved direct enrolment to create the first permanent OneFile."/>:<div className="learner-grid">{learners.map(learner=><article className="learner-card" key={learner.id}><div className="learner-head"><span>{learner.name.split(" ").map(name=>name[0]).join("")}</span><div><h3>{learner.name}</h3><p>{learner.matricule} · {learner.className}</p></div></div><div className="bars"><Bar label="Academic mastery" value={learner.mastery}/><Bar label="Attendance" value={learner.attendance}/><Bar label="Engagement" value={learner.engagement}/><Bar label="Wellbeing" value={learner.wellbeing}/></div><div className="next"><span>Current next action</span><strong>{learner.nextAction}</strong></div><button className="primary" disabled={busyId===learner.id} onClick={()=>open(learner.id)}><FolderHeart/>{busyId===learner.id?"Loading OneFile…":"Open complete OneFile"}</button></article>)}</div>}{selected&&<OneFile data={selected} brand={brand} role={role} onClose={()=>setSelected(null)}/>}</div>}
function Bar({label,value}:{label:string;value:number}){return <div className="bar"><span>{label}<b>{value}%</b></span><i><em style={{width:`${Math.max(0,Math.min(value,100))}%`}}/></i></div>}
function OneFile({data,brand,role,onClose}:{data:LearnerOneFileData;brand:SchoolBrand;role:Role;onClose:()=>void}){const family=role==="parent"||role==="student";return <div className="modal-backdrop"><section className="modal onefile-modal" aria-label={`${data.identity.name} OneFile`}><button className="modal-close" onClick={onClose} aria-label="Close OneFile"><X/></button><header className="onefile-header"><div className="photo">{data.identity.name.split(" ").map(name=>name[0]).join("")}</div><div><span>{brand.shortName} PERMANENT LEARNER RECORD</span><h2>{data.identity.name}</h2><p>{data.identity.matricule} · {data.identity.className}</p></div><button onClick={()=>window.print()}><Printer/>Print authorized summary</button></header><div className="metrics"><Card icon={<IdCard/>} label="Credential" value={data.credential?.status??"Not issued"}/><Card icon={<CalendarCheck/>} label="Attendance" value={`${data.identity.attendanceRate}%`}/><Card icon={<BookOpenCheck/>} label="Assessments" value={String(data.assessments.length)}/><Card icon={<ShieldCheck/>} label="Current risk" value={data.identity.riskLevel}/></div><div className="transport-grid"><Section icon={<UserRound/>} title="Identity"><p>Date of birth: {data.identity.dateOfBirth??"Not recorded"}</p><p>Sex: {data.identity.sex??"Not recorded"}</p><p>Credential valid until: {data.credential?.validUntil||"Not issued"}</p></Section><Section icon={<UsersRound/>} title="Guardians and collection authority">{data.guardians.map((guardian,index)=><article key={`${guardian.name}-${index}`}><strong>{guardian.name}</strong><p>{guardian.relationship}{guardian.isPrimary?" · Primary guardian":""}</p><small>{guardian.phone??guardian.email??"No contact exposed"} · {guardian.canCollect?"May collect":"Not authorized to collect"}</small></article>)}{!data.guardians.length&&<p>No guardian record is visible to this account.</p>}</Section></div><div className="transport-grid"><Section icon={<CalendarCheck/>} title="Attendance evidence">{data.attendance.slice(0,10).map((item,index)=><article key={`${item.recordedAt}-${index}`}><strong>{item.status}</strong><small>{new Date(item.recordedAt).toLocaleDateString()} {item.note&&`· ${item.note}`}</small></article>)}{!data.attendance.length&&<p>No attendance evidence yet.</p>}</Section><Section icon={<BookOpenCheck/>} title="Learning evidence">{data.assessments.slice(0,10).map((item,index)=><article key={`${item.title}-${index}`}><strong>{item.title}</strong><p>{item.score} / {item.maxScore}</p><small>{new Date(item.date).toLocaleDateString()} {item.comment&&`· ${item.comment}`}</small></article>)}{!data.assessments.length&&<p>No published assessment evidence yet.</p>}</Section></div>{!family&&<div className="transport-grid"><Section icon={<FolderHeart/>} title="Support and care">{data.interventions.map((item,index)=><article key={`${item.title}-${index}`}><strong>{item.title}</strong><p>{item.actionPlan}</p><small>{item.status} · review {item.reviewOn}</small></article>)}{data.cases.map(item=><article key={item.number}><strong>{item.number} · {item.title}</strong><p>{item.category} · {item.priority}</p><small>{item.status}</small></article>)}{!data.interventions.length&&!data.cases.length&&<p>No support action visible.</p>}</Section><Section icon={<CircleDollarSign/>} title="Fee-account evidence"><strong>{money(data.finance.balanceDue)} outstanding</strong><p>{money(data.finance.amountPaid)} paid of {money(data.finance.amountDue)}</p>{data.finance.receipts.slice(0,6).map(receipt=><article key={receipt.number}><strong>{receipt.number}</strong><small>{money(receipt.amount)} · {receipt.method} · {new Date(receipt.receivedAt).toLocaleDateString()}</small></article>)}</Section></div>}<Section icon={<BusFront/>} title="Transport assignment">{data.transport?<p>{data.transport.routeName}: {data.transport.pickupStop} → {data.transport.dropoffStop} · {data.transport.status}</p>:<p>No active school transport assignment.</p>}</Section><footer className="id-proof"><BadgeCheck/><span>Loaded from the authoritative school record under current role permissions.</span></footer></section></div>}
function Card({icon,label,value}:{icon:ReactNode;label:string;value:string}){return <article className="metric"><span>{icon}{label}</span><strong>{value}</strong></article>}
function Section({icon,title,children}:{icon:ReactNode;title:string;children:ReactNode}){return <section className="panel"><div className="panel-title">{icon}<div><h3>{title}</h3></div></div>{children}</section>}
